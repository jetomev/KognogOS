# KognogOS greeter. The live ISO autologins (autologin.conf), so this
# mainly matters for the installed system -- but it also means the
# greeter is exercised whenever autologin is turned off in the live
# session, rather than being a code path nobody sees until an install.
[Theme]
Current=kognogos
